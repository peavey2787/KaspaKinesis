import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  // React Compiler can be enabled later with: npm install babel-plugin-react-compiler
  // experimental: {
  //   reactCompiler: true,
  // },
}

export default nextConfig
